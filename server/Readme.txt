Structure of Server Services:

To facilitate builds , the following folder structre and rules should be adopted when building server services.

*** Folder Structure
Server -> 
	decodeService -> 
		
		  ** The main  service project
		  Post method is to convert the encoded hexadecimal value into decoded result
Database:
	No database connection defined